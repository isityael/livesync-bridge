# LiveSync Bridge — Release pipeline
# Triggers on version tags (v*), builds a versioned Docker image,
# signs it, and creates a GitHub release.
when:
  - event: tag
    ref: refs/tags/v*

variables:
  - &image "harbor.m0sh1.cc/apps/livesync-bridge"

steps:
  - name: build-and-push
    image: woodpeckerci/plugin-docker-buildx@sha256:853fafbeaf040b21df556baafe8b1eacb945338f3151ecc286a7f19916f97c00
    privileged: true
    settings:
      registry: harbor.m0sh1.cc
      repo: *image
      dockerfile: Dockerfile
      tags:
        - "${CI_COMMIT_TAG##v}"
        - latest
      logins:
        - registry: harbor.m0sh1.cc
          username:
            from_secret: harbor_username
          password:
            from_secret: harbor_password
        - registry: dhi.io
          username:
            from_secret: dhi_username
          password:
            from_secret: dhi_password
      provenance: mode=max
      buildkit_driveropt: network=host
      build_args:
        - "COMMIT_SHA=${CI_COMMIT_SHA}"
      cache_from: 'type=registry\,ref=harbor.m0sh1.cc/cache/livesync-bridge:buildcache'
      cache_to: 'type=registry,ref=harbor.m0sh1.cc/cache/livesync-bridge:buildcache,mode=max,ignore-error=true'

  - name: trivy-scan
    failure: ignore
    image: dhi.io/trivy:0.70.0-alpine3.23-dev@sha256:18f9194e75e1ba136f9415fffa3246187aca13698a52ab5baf188ec6d3813666
    environment:
      IMAGE_REF: "harbor.m0sh1.cc/apps/livesync-bridge:${CI_COMMIT_TAG##v}"
      TRIVY_USERNAME:
        from_secret: harbor_username
      TRIVY_PASSWORD:
        from_secret: harbor_password
    commands:
      - trivy image --exit-code 1 --severity CRITICAL --ignore-unfixed $${IMAGE_REF}
      - trivy image --exit-code 0 --severity HIGH --ignore-unfixed $${IMAGE_REF}

  - name: sbom-generate
    failure: ignore
    image: dhi.io/syft:1.44.0-alpine3.23-dev@sha256:5827400e1f8401e8b925687aa38624ada774b5480153ddbe342815a68559b8ef
    environment:
      IMAGE_REF: "harbor.m0sh1.cc/apps/livesync-bridge:${CI_COMMIT_TAG##v}"
      SYFT_REGISTRY_AUTH_AUTHORITY: harbor.m0sh1.cc
      SYFT_REGISTRY_AUTH_USERNAME:
        from_secret: harbor_username
      SYFT_REGISTRY_AUTH_PASSWORD:
        from_secret: harbor_password
    commands:
      - syft registry:$${IMAGE_REF} -o spdx-json > sbom.spdx.json

  - name: cosign-sign
    image: dhi.io/cosign:3.0.6-alpine3.23-dev@sha256:c40648f14b1ebcc72216a453ef30c9265ac0c91206b6f65d2a3ba1f940ad7547
    environment:
      IMAGE_REF: "harbor.m0sh1.cc/apps/livesync-bridge:${CI_COMMIT_TAG##v}"
      COSIGN_EXPERIMENTAL: "1"
      COSIGN_KEY:
        from_secret: cosign_key
      COSIGN_PASSWORD:
        from_secret: cosign_password
      REGISTRY_USERNAME:
        from_secret: harbor_username
      REGISTRY_PASSWORD:
        from_secret: harbor_password
    commands:
      - echo "$COSIGN_KEY" > /tmp/cosign.key
      - cosign login harbor.m0sh1.cc -u "$REGISTRY_USERNAME" -p "$REGISTRY_PASSWORD"
      - cosign sign --key /tmp/cosign.key --yes --registry-referrers-mode=oci-1-1 $${IMAGE_REF}
      - cosign attest --key /tmp/cosign.key --type spdxjson --predicate sbom.spdx.json --yes $${IMAGE_REF}
      - rm -f /tmp/cosign.key sbom.spdx.json

  - name: notation-sign
    image: dhi.io/notation:1.3.2-alpine3.23-dev@sha256:f8cb79bd44cfd4865d60c799880bcd962a59727b221aa5a3dcdef2626bf30266
    environment:
      IMAGE_REF: "harbor.m0sh1.cc/apps/livesync-bridge:${CI_COMMIT_TAG##v}"
      NOTATION_KEY:
        from_secret: notation_key
      NOTATION_CERT:
        from_secret: notation_cert
      REGISTRY_USERNAME:
        from_secret: harbor_username
      REGISTRY_PASSWORD:
        from_secret: harbor_password
    commands:
      - mkdir -p /tmp/notation $HOME/.config/notation $HOME/.docker
      - echo "$NOTATION_KEY" > /tmp/notation/harbor.key
      - echo "$NOTATION_CERT" > /tmp/notation/harbor.crt
      - |
        cat > $HOME/.config/notation/signingkeys.json <<EOF
        {"default":"harbor","keys":[{"name":"harbor","keyPath":"/tmp/notation/harbor.key","certPath":"/tmp/notation/harbor.crt"}]}
        EOF
      - printf '{"auths":{"harbor.m0sh1.cc":{"auth":"%s"}}}' "$(printf '%s:%s' "$REGISTRY_USERNAME" "$REGISTRY_PASSWORD" | base64 -w0)" > $HOME/.docker/config.json
      - notation sign --force-referrers-tag=false --key harbor $${IMAGE_REF}
      - rm -rf /tmp/notation $HOME/.docker/config.json

  - name: github-release
    image: woodpeckerci/plugin-release@sha256:7b8cddaf649aebee4a78b278c4a3c92ec6171638211514bdf9c095000108444d
    settings:
      api_key:
        from_secret: github_token
      base_url: https://api.github.com
      title: "Release ${CI_COMMIT_TAG}"
      note: "Docker image: `harbor.m0sh1.cc/apps/livesync-bridge:${CI_COMMIT_TAG##v}`"
